# Local-only handoff bundle (re-fetch from Claude Design when needed; link expires)
handoff.tar.gz
_handoff_extract/

# Junction to the OneDrive project documents folder (local context only)
project-docs/

# OS noise
Thumbs.db
.DS_Store

# Build pipeline scratch (downloaded/extracted handoff)
.work/

# Download-ZIP handoff bundles the user drops here (~300 MB each; re-download from Claude
# Design when needed). Extract with Expand-Archive, build with --src, then delete.
version/

# Standalone single-file export (large; generated on demand via make-standalone.mjs, share directly)
SpatialTimber-Deck-*-standalone.html
